/**
 * Client Configuration
 * 
 * Copy this file to config.js and update with your actual values.
 * config.js is in .gitignore and should never be committed.
 */

window.TANGERINE_CONFIG = {
  // n8n webhook URL for the chat widget
  n8nWebhookUrl: 'https://n8n.your-instance.com/webhook/tangerine-test',

  // Secure reporting endpoint URL (for example, your Supabase Edge Function URL)
  reportingApiUrl: 'https://your-project-ref.supabase.co/functions/v1/reporting-dashboard',

  // Optional: row limit sent to the reporting endpoint (0 = fetch all rows)
  reportingLimit: 0
};
