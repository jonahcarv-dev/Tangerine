/**
 * Client Configuration
 * 
 * Copy this file to config.js and update with your actual values.
 * config.js is in .gitignore and should never be committed.
 */

window.TANGERINE_CONFIG = {
  // n8n webhook URL for the chat widget
  n8nWebhookUrl: 'https://n8n.your-instance.com/webhook/tangerine-test',

  // Supabase URL for reporting dashboard (browser-safe, public project URL)
  supabaseUrl: 'https://your-project-ref.supabase.co',

  // Supabase ANON key only (never use service_role key in browser code)
  supabaseAnonKey: 'your-anon-key',

  // Optional: override table name if needed
  chatHistoriesTable: 'Chat histories'
};
